# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated image sequences (delivered as ZIPs, not tracked in git)
public/sequence-12fps/
public/sequence-24fps/
public/sequences/*.zip
# WebP frames under public/sequences/ are tracked — served as static assets

# Throwaway measurement probes written at the repo root during debugging.
# Two of these have been committed by accident via `git add -A`.
/.*.mjs
