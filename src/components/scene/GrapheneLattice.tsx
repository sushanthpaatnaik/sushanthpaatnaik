// GrapheneLattice — semantic alias for the WebGL graphene atmosphere.
// Re-exports the existing implementation so consumers can import either name.
export { default } from "./GrapheneAtmosphere";
export { default as GrapheneAtmosphere } from "./GrapheneAtmosphere";
